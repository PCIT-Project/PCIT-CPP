def std = @import("std");

type StaticArrayList = alias @import("StaticArrayList.pthr").StaticArrayList;


func entry = () #entry -> UI8 {
	var arr = new StaticArrayList<{Int, 3}>();
	arr.append().* = 1;
	arr.append().* = 2;
	arr.append().* = 3;

	var arr_2: StaticArrayList<{Int, 3}> = copy arr;

	return arr_2.last().* as UI8;
}