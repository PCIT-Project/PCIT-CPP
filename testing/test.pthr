def std_exp = @import("std_exp/std_exp.pthr");


type NoReloc = struct {
	func copy = delete;
}



func entry = () #entry -> UI8 {
	var num: Int = 12;

	var num_atomic = new std_exp.AtomicRef<{Int}>(&num);

	// _ = num_atomic.fetchAdd(2);

	// _ = @atomicRMW<{Int*, Int, @pthr.AtomicRMWOp.ADD, @pthr.AtomicOrdering.SEQ_CST}>(&num, 2);

	
	return num as UI8;
}