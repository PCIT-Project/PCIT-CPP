
type ErrNum = struct {
	func new = () -> (output: ErrNum) <Void> {
		error;
	}

	func new = (num: UI8) -> (output: ErrNum) {
		output.num = copy num;
		return...;
	}


	const num: UI8;
}




func entry = () #entry -> UI8 {
	const num: ErrNum = try new ErrNum() else new ErrNum(12);

	return copy num.num;
}