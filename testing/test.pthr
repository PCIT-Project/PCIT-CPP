def std_exp = @import("std_exp/std_exp.pthr");



func entry = () #entry -> UI8 {
	const arr = new [Int:3][1, 2, 3];

	const arr_ref: [Int:*] = arr as [Int:*];

	var sum: Int = 0;


	// for(arr_ref) [i: USize; value: Int] {
	// 	// value += i as Int;
	// }

	for(arr) [_; value: Int] {
		sum += value;
	}

	return sum as UI8;
}

