def std = @import("std");

type StaticString = alias @import("StaticString.pthr").StaticString;


const global_str: [Char:11;'\0'] = get_string();




func get_string = () -> (output: [Char:11;'\0']) {
	// var str = new StaticString<{12}>();

	output = new [Char:11;'\0']();

	for(output as [Char:*mut], "asdf".* as [Char:*]) [_; dst: Char mut, src: Char] {
		
	}

	// std.mem.copyBuffer(output as [Char:*mut], "Hello World");

	return...;
}



func entry = () #entry -> UI8 {
	const foo = get_string();
	
	return 0;
}
