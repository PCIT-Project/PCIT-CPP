

func iter = (arr: [UInt:*]) -> Void {
	for(arr) [i: USize; value: UInt] {
		const v1: UInt = 12;
		const v2: UInt = 12;
	}
}



func entry = () #entry -> UI8 {
	var arr = new [UInt:3][1, 2, 3];

	iter(arr);

	return 0;
}

