def c_import = @importCPP("test.h");



func entry = () #entry -> UI8 {
	var num: UI8* = c_import.malloc(@numBytes<{UI8}>()) as UI8*;
	defer{ c_import.free(num as RawPtr); }

	num.* = 12;

	return copy num.*;
}