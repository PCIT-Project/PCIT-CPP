type Struct = struct {
	var foo: Int;
	var bar: Int;
}

func discard = (val: $$ read) -> Void {
	
}


func entry = () #entry -> UI8 {
	var foo: Int = 12;

	discard(copy foo);
	discard(&foo);
	discard(new Struct{foo = 12, bar = 14});

	
	return 0;
}

