type StaticArrayList = alias @import("StaticArrayList.pthr").StaticArrayList;


func entry = () #entry -> UI8 {
	const arr = new StaticArrayList<{Int, 4}>(new [Int:3][1, 2, 3]);

	var total: Int = 0;
	for(arr) [_; value: Int] {
		total += value;
	}
	
	return total as UI8; // 6
}