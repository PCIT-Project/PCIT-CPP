

func entry = () #entry -> UI8 {
	var arr = new [Int:4][1, 2, 3, 4];
	const coefs = new [Int:4][1, 0, 2, 5];
	
	for(arr, coefs) [_; val: $$ mut, coef: Int] {
		val *= coef;
	}
	
	return (arr == new [Int:4][1, 0, 6, 20]) as UI8;
}