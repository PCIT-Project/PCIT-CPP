

var bar: UI8 = get_value(12);


def USE_RVO: Bool = false;

when(USE_RVO){
	func get_value = (input: UI8) -> (output: UI8) {
		output = copy input;
		return...;
	}
	
}else{
	func get_value = (input: UI8) -> UI8 {
		return copy input;
	}
}

