// def std_exp = @import("std_exp/std_exp.pthr");





func entry = () #entry -> UI8 {
	type Foo = struct {
		type Bar = struct {
			func [] = (this, value: USize) -> USize { return copy value; }
		}
		func [] = (this, value: USize) -> USize { return copy value; }
	}
	

	const arr = new [Int:3][1, 2, 3];
	for(arr) [_; value: Int] {}


	// const foo: Foo.Bar = false;
	
	return 0;
}