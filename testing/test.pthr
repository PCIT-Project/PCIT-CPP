

func get_num = (num: UInt) -> UInt {
	return copy num;
}

func get_num = (num: Int) -> Int = delete("Only unsigned");


func entry = () #entry -> UI8 {
	_ = get_num(12 as UInt);
	_ = get_num(12 as Int);

	
	return 0;
}